name: BATS Tests

on:
  push:
    branches: [main, test]
  pull_request:
    branches: [main, 0.1.0-rc.1]

jobs:
  bats:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install jq >= 1.8
        run: |
          mkdir -p "$RUNNER_TEMP/bin"
          curl -L https://github.com/jqlang/jq/releases/download/jq-1.8.1/jq-linux-amd64 -o "$RUNNER_TEMP/bin/jq"
          echo "020468de7539ce70ef1bceaf7cde2e8c4f2ca6c3afb84642aabc5c97d9fc2a0d  $RUNNER_TEMP/bin/jq" | sha256sum -c
          chmod +x "$RUNNER_TEMP/bin/jq"
          echo "$RUNNER_TEMP/bin" >> "$GITHUB_PATH"
          jq --version

      - name: Run BATS tests
        run: ./test/run-tests.sh

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bats-results
          path: test-results/
          retention-days: 30
